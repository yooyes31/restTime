---
id: 10
title: Google 로그인 + Drive 불러오기/저장하기
category: enhancement
state: done
blocked_by: []
type: AFK
user_stories: [1, 2, 3, 4, 5, 43, 44, 45]
---

# Google 로그인 + Drive sync

## What to build

GIS 로그인/로그아웃. 설정: Drive pull/push, remote newer 경고, protein_factor. 오프라인 로컬 기록. 백업: `restTime/health.db`. **본인 계정 데이터만.**

## Acceptance criteria

- [x] sign-in/out
- [x] pull / push 버튼
- [x] remote newer 경고
- [x] protein_factor 설정
- [x] 계정 전환 시 로컬 DB 처리

---

## Agent Brief

> *This was generated by AI during triage.*

**Category:** enhancement  
**Summary:** Google sign-in and manual Drive DB sync with conflict warning

**Current behavior:** GIS 토큰 + Drive API v3 로 `restTime/health.db` 수동 동기화, 설정 화면에서 수행.

**Desired behavior:** User signs in with Google; Settings has pull/push; importBlob/exportBlob via Drive API; warn if remote newer before push; protein_factor in settings; offline local edits OK; each Google account sees only their Drive backup; handle account switch (clear or prompt before loading other user's DB).

**Key interfaces:**

- `GoogleAuthService` — signIn, signOut, getAccessToken
- `GoogleDriveSyncService` — pull, push, checkRemoteNewer
- Backup path: `restTime/health.db`

**Acceptance criteria:**

- [x] Login/logout works with #9 Client ID
- [x] Manual pull and push
- [x] Remote-newer warning before overwrite
- [x] protein_factor persisted
- [x] Account isolation via per-user Drive file

**Out of scope:**

- Auto background sync
- Multi-user server DB (v2)
