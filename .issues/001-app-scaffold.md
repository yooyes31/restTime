---
id: 1
title: App scaffold + SQLite foundation
category: enhancement
state: done
blocked_by: []
type: AFK
user_stories: [6, 49]
---

# App scaffold + SQLite foundation

## What to build

Vue 3 + Vite + Element Plus + TypeScript PWA 뼈대. 브라우저 SQLite(LocalDatabase) 초기화, v1 전체 schema migration, `exportBlob()` / `importBlob()`. 라우터: 오늘/주간/설정 등 빈 화면.

## Acceptance criteria

- [x] `npm run dev` 실행, PWA manifest 기본
- [x] SQLite WASM + schema (meal, workout, inbody, settings)
- [x] 새로고침 후 DB 유지 (IndexedDB + sql.js; OPFS 미사용)
- [x] exportBlob / importBlob 동작 (설정 화면 디버그 버튼)
- [x] 빈 「오늘」 화면 + 네비 골격

---

## Agent Brief

> *This was generated by AI during triage.*

**Category:** enhancement  
**Summary:** Greenfield PWA shell with browser SQLite and full v1 schema

**Current behavior:** Empty repo — no app.

**Desired behavior:** Runnable Vue PWA; LocalDatabase initializes on boot; all v1 tables exist; DB survives refresh; blob export/import works for future Drive sync; empty routed pages for Today, Week, Settings.

**Key interfaces:**

- `LocalDatabase` — `query()`, `exec()`, `exportBlob()`, `importBlob()`
- Schema tables: `meal_entries`, `meal_presets`, `workout_sessions`, `workout_presets`, `workout_preset_items`, `inbody_logs`, `settings`

**Acceptance criteria:**

- [x] Dev server runs; PWA manifest present
- [x] Migrations apply all PRD tables
- [x] Data persists across refresh
- [x] exportBlob/importBlob round-trip verified (Settings debug UI)
- [x] Router skeleton with placeholder views

**Out of scope:**

- Meal/workout/inbody business UI (#2–#8)
- Google OAuth (#9–#10)
- Production deploy (#11)
